version: 0.0.27
scripts:
  bootstrap: |
    poetry install --no-root
    apt update && apt install -y ffmpeg
icon: "./icon.png"
name: media-downloader
description: "%a-feature-rich-audio-video-downloader-youtube-viemo-tiktok%"
displayName: "%mediadownloaer%"
dependencies:
  {}
repository: https://github.com/vince-hz/yt-dlp-downloader
releaseNotes: Cookie content print
